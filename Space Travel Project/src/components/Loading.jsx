import "../styling/Loading.css";

//a loading component to manage API response times

const Loading = () => {
  return (
    <div className="Loading">
      <p>Loading...</p>
    </div>
  );
};
export default Loading;
